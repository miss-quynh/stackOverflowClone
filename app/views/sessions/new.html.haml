%h2 Sign In Form

= form_for(:session, url: sessions_path) do |f|
  %h4
    = f.label :username
    = f.text_field :username

  %h4
    = f.label :password
    = f.password_field :password

  = f.submit
